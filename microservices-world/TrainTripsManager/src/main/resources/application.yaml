spring:
  application:
    name: TrainTripsManager

server:
  port: 8081

springdoc:
  swagger-ui:
    url=: /train-api.yaml


eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/